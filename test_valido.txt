programaprueba

{{ prueba:  comentario de varias lineas (tema 16). }}

ulongint contador, total;
doublef promedio;

typedef color = [1, 2, 3];

class figura
begin
    ulongint lados;
    doublef area;
end;

figura fig;

ulongint function duplicar(ulongint valor)
begin
    ret(valor * 2$ul);
end;

begin
    contador := 10$ul;
    total := contador + 5$ul - 3$ul;
    promedio := 3.14;
    promedio := todf(total);
    fig.lados := 4$ul;
    total := duplicar(contador)[1];
    total := contador = (7$ul);
    pout({hola mundo});
    if (contador > 5$ul)
        pout({es mayor});
    else
        pout({es menor});
    end_if;
    repeat
        contador := contador - 1$ul;
    while (contador > 0$ul);
end
