{{ Programa valido: temas 6 8 9 13 16 17 19 23 25 28 31 33 }}
prog
ulongint x, y, w;
doublef z;
typedef pares = [2$ul, 4$ul, 6$ul];
pares p1, p2;

class base
begin
    ulongint a;
    ulongint m(ulongint pa)
    begin
        ret(pa);
    end;
end;

class hija
begin
    ulongint b;
    extends base;
end;

ulongint function foo(ulongint n, doublef d)
begin
    ret(n);
end;

begin
    x := 0$ul;
    y := 4294967295$ul;
    z := 1.0;
    z := .6d-1;
    x := y = (2$ul);
    x := y = (x + y) * w = (1$ul);
    if (x = (1$ul) == 1$ul)
        pout({ok});
    end_if;
    z := todf(x);
    x := foo(n=1$ul, 2.5)[1$ul, 2$ul];
    p1 := 2$ul;
    repeat
        x := x + 1$ul;
    while (x < 3$ul);
    if (x > 0$ul)
        pout({hola});
    else
        pout(x);
    end_if;
end
